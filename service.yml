apiVersion: v1
kind: Service
metadata:
  name: conversion-api
  namespace: gitlab-managed-apps
spec:
  selector:
    app: conversion
  ports:
    - protocol: TCP
      port: 12345
      targetPort: 12345
